// import React from 'react';

// const MessageBox = props => props.message && (
//     <div>
//       <div className={typeof props.message === 'string' ? 'messageDisplay' : 'errorDisplay'}>
//         <div className="alert">
//           {typeof props.message === 'string'
//             ? props.message
//             : props.message.data.map((error, index) => <li key={index}>{error.msg}</li>)}
//         </div>
//       </div>
//     </div>
// );

// export default MessageBox;
